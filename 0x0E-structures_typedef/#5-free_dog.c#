#include "dog.h"
#include <stdlib.h>

/**
 * free_dog - Function frees dog
 * @d: pointer to dog_t structure
 *
 */

void free_dog(dog_t *d)
{
	*d = &dog;
	free(dog->name);
	free(dog->owner);
	free(dog);
}
